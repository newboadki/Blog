<% rows = matrix_rows(@apps, 4) %>

<ul class="row post-list">

	<%= render :partial => 'rows', :collection => rows, :as => 'row' %>

</ul>


<ul class="app-descriptions-archive">

	<%= render :partial => 'app_description_archive_row', :collection => @apps, :as => 'app' %>
	
</ul>

<script>
	addExpandableRowBehaviourToAppLinks();
</script>